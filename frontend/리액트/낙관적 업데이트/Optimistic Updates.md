# 낙관적 업데이트

낙관적 업데이트란 서버의 응답을 기다리지 않고 요청이 정상이라고 예상하고 ui를 먼저 업데이트 한뒤 나중에 서버 요청이 실패하면 다시 되돌리는 방식입니다.

## 왜 쓸까?

- 버튼을 눌렀을 때 바로 반응 해서 앱이 빠르게 느껴져서 사용자 경험을 개선할 수 있음
- 네트워크 지연을 사용자가 체감 하지 못하게 할 수 있음
- 좋아요나 댓글 기능에서 자연스러운 인터랙션을 느낄 수 있음

## 예제 코드

```
import { useState } from "react";

export default function LikeButton() {
  const [liked, setLiked] = useState(false);

  const toggleLike = async () => {
    const prev = liked;

    setLiked(!liked);

    try {
      const res = await fetch("/api/like", { method: "POST" });
      if (!res.ok) throw new Error();
    } catch {
      setLiked(prev);
      alert("좋아요 실패");
    }
  };

  return <button onClick={toggleLike}>{liked ? "❤️" : "🤍"}</button>;
}
```

<video controls src="20260129-0755-36.5795555-1.mp4" title="Title"></video>

이런식으로 낙관적 업데이트를 사용하면 ui업데이트를 먼저 한뒤 요청이 실패하면 다시 되돌립니다. 따라서 사용자 입장에서 딜레이가 없게 느껴진다는 장점이 있습니다.
